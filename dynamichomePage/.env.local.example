# Copy this file to .env.local (gitignored) and customise for your local setup.

# Set to true to bypass Cognito authentication on protected routes.
# Allows testing all pages locally without a real Cognito session.
# VITE_DEV_SKIP_AUTH=true
